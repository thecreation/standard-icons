{
  "name": "{{name}}",
  "title": "{{title}}",
  {{#if description}}
  "description": "{{description}}",
  {{/if}}
  "version": "{{version}}",
  "class": "{{class}}",
  "prefix": "{{prefix}}",
  "homepage": "{{homepage}}",
  "author": "{{author}}",
  "license": "{{license}}",
  "style": "{{name}}.css",
  "count": "{{count}}",
  "classifiable": {{classifiable}},
  "icons": {
    {{#each icons}}
    {{#equal @index ../icons}}
    "{{name}}": "{{title}}"
    {{else}}
    "{{name}}": "{{title}}",
    {{/equal}}
    {{/each}}
  }
  {{#if classifiable}}
  ,
  "categories": {
    {{#each categories}}
    {{#equal @index ../categories}}
    "{{@key}}": [
      {{#each this}}
      {{#equal @index ../this}}
      "{{this}}"
      {{else}}
      "{{this}}",
      {{/equal}}
      {{/each}}
    ]
    {{else}}
    "{{@key}}": [
      {{#each this}}
      {{#equal @index ../this}}
      "{{this}}"
      {{else}}
      "{{this}}",
      {{/equal}}
      {{/each}}
    ],
    {{/equal}}
    {{/each}}
  }
  {{/if}}
}