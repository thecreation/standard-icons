#!/usr/bin/env node

const argv = require('yargs').argv;
const colors = require('colors');

console.info(argv.module);

let importer = require(`../../modules/${argv.module}/importer`);
importer();

console.log(colors.green(`${argv.module} imported.`));